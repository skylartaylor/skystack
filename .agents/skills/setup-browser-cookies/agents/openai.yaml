interface:
  display_name: "Setup Browser Cookies"
  short_description: "Import auth cookies from a real browser into browse"
  default_prompt: "Use $setup-browser-cookies to authenticate the headless browser."

policy:
  allow_implicit_invocation: true
